import 'package:flutter/material.dart';


class AppColors {
  // Shared Primary Color
  static const Color primary = Color(0xFFE47927);

  // Light Theme
  static const Color primaryLight = primary;
  static const Color secondaryLight = Colors.orange; // can customize later
  static const Color backgroundLight = Colors.white;
  static const Color textPrimaryLight = Colors.black;
  static const Color textSecondaryLight = Colors.grey;

  // Dark Theme
  static const Color primaryDark = primary;
  static const Color secondaryDark = Colors.orange; // keep consistent with light
  static const Color backgroundDark = Colors.black;
  static const Color textPrimaryDark = Colors.white;
  static const Color textSecondaryDark = Colors.grey;
}

